import assert from 'node:assert/strict';import test from 'node:test';import{createPathEntityEditor}from'../src/application/path-entity-editor.ts';
const NOW='2026-09-02T08:00:00.000Z';const entity=(id,type='goal',extra={})=>({id,type,title:id,description:'',status:'todo',createdAt:NOW,updatedAt:NOW,tags:[],extensions:{legacy:true},...extra});const document=entities=>({repositoryVersion:1,envelope:{schemaVersion:1,revision:0,entities,events:[],extensions:{}},settings:{},extensions:{}});const draft=(id,extra={})=>({id,title:'Titre',description:'Description',priority:'high',tags:['x'],status:'todo',extensions:{legacy:{kept:true}},...extra});
function harness(initial){let current=structuredClone(initial),writes=0,after=0,failure,ids=0;const editor=createPathEntityEditor({async updateDocument(fn){const candidate=await fn(structuredClone(current));if(!candidate)return structuredClone(current);if(failure)throw failure;candidate.envelope.revision=current.envelope.revision+1;current=structuredClone(candidate);writes++;return structuredClone(current)},afterPersist(){after++},now:()=>NOW,createId:()=>`event-${++ids}`});return{editor,snapshot:()=>structuredClone(current),counts:()=>({writes,after}),fail:v=>failure=v}}
test('create and multi-field edit each use one write',async()=>{const r=harness(document([entity('goal')]));assert.equal((await r.editor.saveAction(draft('action',{parentId:'goal',planned:{start:'2026-09-02'},status:'done'}),'create')).accepted,true);assert.deepEqual(r.counts(),{writes:1,after:1});assert.equal((await r.editor.saveAction(draft('action',{title:'Nouveau',priority:'low',tags:['y'],parentId:'goal',planned:{start:'2026-09-03'},status:'in-progress'}),'edit')).accepted,true);assert.deepEqual(r.counts(),{writes:2,after:2});const e=r.snapshot().envelope.entities.find(x=>x.id==='action');assert.equal(e.title,'Nouveau');assert.deepEqual(e.extensions,{legacy:{kept:true}})});
test('middle rejection abandons candidate',async()=>{const r=harness(document([entity('action','action',{title:'Avant',planned:{start:'2026-09-02'}})])),before=r.snapshot();assert.deepEqual(await r.editor.saveAction(draft('action',{title:'Après',planned:undefined}),'bad'),{accepted:false,reason:'invalid-command'});assert.deepEqual(r.snapshot(),before);assert.deepEqual(r.counts(),{writes:0,after:0})});
test('failure retry and replay preserve id',async()=>{const r=harness(document([])),x=draft('stable');r.fail(new Error('disk'));await assert.rejects(()=>r.editor.saveGoal(x,'cmd'),/disk/);assert.deepEqual(r.counts(),{writes:0,after:0});r.fail();assert.equal((await r.editor.saveGoal(x,'cmd')).entityId,'stable');assert.equal((await r.editor.saveGoal(x,'cmd')).replayed,true);assert.deepEqual(r.counts(),{writes:1,after:1});assert.deepEqual(await r.editor.saveGoal({...x,title:'Other'},'cmd'),{accepted:false,reason:'invalid-command'})});
test('archive refuses living children then tombstones leaf',async()=>{const r=harness(document([entity('goal'),entity('action','action',{parentId:'goal'})]));assert.deepEqual(await r.editor.archive('goal','g'),{accepted:false,reason:'has-children'});assert.equal((await r.editor.archive('action','a')).accepted,true);assert.equal(r.snapshot().envelope.entities.find(x=>x.id==='action').deletedAt,NOW)});
